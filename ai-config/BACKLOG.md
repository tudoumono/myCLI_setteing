# Backlog

保留中のアイデアや、条件付きで将来実装する案を記録する。

## ルール

- 案ごとに「トリガー条件」を明記する（いつ再検討するか）
- 実装したら「完了」へ移動し、対応コミットを記載

## 保留中

### タイムスタンプによるスキル陳腐化検知

- **トリガー**: スキル数が20を超えた時点
- **概要**: `status` コマンドに各スキルの last modified（git日付）を表示
- **経緯**: skill-discovery導入時に検討。スキル9個の現状では管理コストが上回るため見送り

### スキル配布の targets 制御（方針C）

- **トリガー**: Gemini/Codex でスキル互換性の実害が出た時点
- **概要**: SKILL.md frontmatter に `targets: [claude]` 等を追加し、sync_skills_to_target でフィルタ
- **経緯**: CLI非依存化（方針B）で当面は対処可能と判断

### rejected.yml（スキル候補の棄却履歴）

- **トリガー**: skill-discovery が同じ候補を繰り返し提案して煩わしい時点
- **概要**: 棄却した候補を記録し再提案を防止
- **経緯**: 保管場所（共有 vs CLI別）の設計が未決。運用してから判断

## 完了

（なし）
